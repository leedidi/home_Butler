# 개발 작업 규칙

## 브랜치 운영

- 모든 개발 작업은 `develop` 브랜치에서 진행한다.
- `main` 브랜치에서는 코드를 직접 수정하지 않는다.
- 작업을 시작하기 전에 현재 브랜치가 `develop`인지 확인한다.
- QA 수정도 `develop` 브랜치에서 진행한다.
- QA를 통과한 작업만 `main` 브랜치에 병합한다.
- `main` 브랜치 병합과 push는 사용자가 명시적으로 요청한 경우에만 진행한다.

## 구현 범위

- 기존 Lovable UI를 임의로 전체 리디자인하지 않는다.
- PRD에 없는 기능을 임의로 추가하지 않는다.
- 한 번에 여러 STEP을 구현하지 않는다. 사용자가 지정한 STEP만 작업한다.

## 테스트 및 완료

- 각 기능을 구현한 후 테스트 가능한 상태인지 확인한다.
- 개발이 끝난 작업은 커밋하고 `develop` 브랜치에 push한다.
- 기능 구현 후 다음 내용을 사용자에게 보고한다.
  - 변경된 파일
  - 테스트 결과
  - 남아 있는 이슈
